# Machine-/site-specific — never distribute these
# the saved gateway hostname (per user)
.citrix-host
# fetched certificates (contain internal hostnames)
certs/
# run logs
logs.txt

# Large, licensed Citrix downloads, shared by both VMs (see citrix_files/README.md)
citrix_files/*.deb
citrix_files/*.tar.gz

flake.lock
.direnv
.vagrant
